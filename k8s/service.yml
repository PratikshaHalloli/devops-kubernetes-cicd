apiVersion: v1
kind: Service
metadata:
  name: devops-demo-api
  namespace: devops-demo
spec:
  type: LoadBalancer
  selector:
    app: devops-demo-api
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
